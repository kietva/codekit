# Coder Toolkit: Implement

Read the approved coder `input.md`, the automatically matched approved prototype, and `implementation.md`. Implement only the approved scope. Preserve prototype behavior, map fields to real data, enforce action rules and permissions, connect the API contract, handle all relevant states, add regression tests, and run project checks. Update task status and implementation notes. Stop and report if the source architecture or API conflicts with the package.
