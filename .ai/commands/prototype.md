# Prototype Toolkit: Create

Input: `prototype/P-<DOMAIN>-<NUMBER>/request.md`.

Read the request, project architecture, component library, existing routes, layout, theme, and relevant source. Create one runnable UI prototype using existing components and mock data. Do not implement production API, persistence, authentication, or business logic. Include relevant loading, empty, error, success, and interaction states. Record route, files, assumptions, and review notes in the package `output.md`.
